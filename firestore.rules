
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS 100% ABIERTAS PARA PRUEBAS Y DEPURACIÓN
    // Forzando despliegue inmediato (Timestamp): 2025-02-18 10:30:00 (UTC)
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
