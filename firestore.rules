
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS 100% ABIERTAS PARA DEPUBACIÓN
    // Forzando despliegue para refrescar caché: 2024-05-21 13:15:00 (UTC)
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
