
{
  "entities": {
    "Property": {
      "title": "Property",
      "description": "Representa una propiedad inmobiliaria (casa, apartamento, local).",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "price": { "type": "number" },
        "type": { "enum": ["Casa", "Apartamento", "Local Comercial", "Oficina", "Quinta"] },
        "operationType": { "enum": ["Venta", "Alquiler"], "description": "Define si la propiedad está para vender o alquilar." },
        "status": { "enum": ["Disponible", "Vendido", "Alquilado"], "default": "Disponible" },
        "province": { "enum": ["San José", "Alajuela", "Cartago", "Heredia", "Guanacaste", "Puntarenas", "Limón"] },
        "city": { "type": "string" },
        "bedrooms": { "type": "number" },
        "bathrooms": { "type": "number" },
        "parking": { "type": "number" },
        "area_m2": { "type": "number" },
        "imageUrls": { "type": "array", "items": { "type": "string" } },
        "features": { "type": "array", "items": { "type": "string" } },
        "mapUrl": { "type": "string" },
        "soldAt": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "title", "price", "type", "province", "city", "imageUrls", "operationType"]
    },
    "Lot": {
      "title": "Lot",
      "description": "Representa un lote o quinta en venta.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "price": { "type": "number" },
        "lotType": { "enum": ["Lote", "Quinta"], "default": "Lote" },
        "province": { "enum": ["San José", "Alajuela", "Cartago", "Heredia", "Guanacaste", "Puntarenas", "Limón"] },
        "city": { "type": "string" },
        "area_m2": { "type": "number" },
        "topography": { "type": "string" },
        "status": { "enum": ["Disponible", "Vendido"], "default": "Disponible" },
        "imageUrls": { "type": "array", "items": { "type": "string" } },
        "features": { "type": "array", "items": { "type": "string" } },
        "mapUrl": { "type": "string" },
        "soldAt": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "title", "price", "lotType", "province", "city", "area_m2", "imageUrls"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/properties/{propertyId}": {
      "schema": { "$ref": "#/entities/Property" },
      "description": "Colección pública de propiedades disponibles."
    },
    "/lots/{lotId}": {
      "schema": { "$ref": "#/entities/Lot" },
      "description": "Colección pública de lotes y terrenos."
    }
  }
}
